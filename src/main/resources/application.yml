server:
  port: 8080

spring:
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/blog
        username: root
        password: root
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true